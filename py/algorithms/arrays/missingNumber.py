def missingNo(arr, n):
    # naturals numbers
    total = (n+1)*(n+2)/2
    mySum = sum(arr)
    return total - mySum


def missingNo2(arr, n):
    temp = [0] * N-1
    for i in range(0, N):
        temp[]


arr = [1,2,3,5]
n = len(arr)

print(missingNo(arr, n))
