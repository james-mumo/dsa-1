package Algorithims;

public class InsertionSort {
    static void insertionSort(int[] arr) {
        int n = arr.length;
        for (int i = 1; i < n; i++) {
            int key = arr[i];
            int currIndex = i + 1; //to the right of key
            while (currIndex >= 0 && key > arr[currIndex]){

            }
        }
    }


    public static void main(String[] args) {

    }
}
